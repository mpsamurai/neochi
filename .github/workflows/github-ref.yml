name: GITHUB_REF test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name:
        run: |
          echo $GITHUB_REF | sed 's/refs\/heads\///g'